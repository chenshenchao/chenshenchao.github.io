# NestJs 速查

NestJs 有一种要成为 NodeJs 里 Spring boot 的趋势，但是用 NodeJs 开发后端还是太小众了。

```bash
# 安装命令行工具
npm i -g @nestjs/cli

# 创建项目
nest new project-name
```

## 常用库

- @nestjs/config 配置，加载 .env 等。
- @nestjs/swagger 文档，OpenAPI 服务。
