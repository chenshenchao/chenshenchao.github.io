# [草稿] rustfs 速查

- [rustfs](https://github.com/rustfs/rustfs)
- [download](https://rustfs.com/download) 下载页面，提供图形化的工具但还不成熟。

中国团队用 rust 实现的高性能分布式对象存储（在 4KB 对象负载场景下，速度比 MinIO 快 2.3 倍），支持 S3 基础协议。

```bash
# 提供了六七种安装方式。
# 1. 命令安装（没试过，我直接通过下载用的安装版）
curl -O https://rustfs.com/install_rustfs.sh && bash install_rustfs.sh

# 其他略...
```

